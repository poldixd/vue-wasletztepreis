image: node:latest

pages:
  cache:
    paths:
    - node_modules/

  script:
  - npm ci
  - npm run build
  - rm -fR ./public
  - mv ./dist ./public

  artifacts:
    paths:
    - public
  
  only:
  - master
